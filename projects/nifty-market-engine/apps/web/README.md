# Dashboard

Deploy this directory as the Vercel project root.

Browser Realtime: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`.
Optional server endpoint: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`.
Never prefix the service-role key with `NEXT_PUBLIC_`.
